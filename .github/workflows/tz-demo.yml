name: "*CRITICAL* <https://evil.example|click here> ~ok~ `code` & <script>alert(1)</script> payments"
on:
  schedule:
    - cron: "30 5 * * *"
      timezone: "America/New_York"
  workflow_dispatch: {}

jobs:
  t:
    runs-on: ubuntu-latest
    steps:
      - run: echo "timezone-aware schedule"
